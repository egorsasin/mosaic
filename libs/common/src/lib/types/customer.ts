export type CreateCustomerInput = {
  customFields?: string;
  emailAddress: string;
  firstName: string;
  lastName: string;
  phoneNumber: string;
};

export type Customer = {
  customFields?: string;
  emailAddress: string;
  firstName: string;
  lastName: string;
  phoneNumber: string;
};
