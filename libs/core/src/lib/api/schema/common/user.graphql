type User implements Node {
  id: Int!
  createdAt: DateTime!
  updatedAt: DateTime!
  identifier: String!
  verified: Boolean!
  lastLogin: DateTime
  #authenticationMethods: [AuthenticationMethod!]!
}
